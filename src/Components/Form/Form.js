//Reusable component that handles all text input fields in order to create a new event. This should also use handlechange and onchange as well as manage state.
import React, { useState } from "react";
import "./Form.css";
import Paper from "@mui/material/Paper";
import IconButton from "@mui/material/IconButton";
import Button from "@mui/material/Button";
import TextField from "@mui/material/TextField";

function Form() {
	return <div></div>;
}

export default Form;
